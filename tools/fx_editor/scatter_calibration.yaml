# FX scatter layout: gameplay meters vs NDF XY units
#
# Confirmed conversion (reference: fx_impact_sol_HE_M270_227mm_Cluster_1.ndf):
# - anchor_max_ndf_radius = max hypot(dx,dy) over Mobile Position XY offsets in that file (NDF units).
# - reference_gameplay_radius_m = gameplay radius (m) assigned to that same farthest offset.
#
# gameplay_xy = ndf_xy * (reference_gameplay_radius_m / anchor_max_ndf_radius)
# ndf_xy = gameplay_xy * (anchor_max_ndf_radius / reference_gameplay_radius_m)

reference_gameplay_radius_m: 60.0
anchor_max_ndf_radius: 4240.282686
